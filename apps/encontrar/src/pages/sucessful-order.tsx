import React from 'react';

import { SuccessfulOrder } from 'modules/CheckoutPage/SuccessfulOrder';

const SucessfullOrder = () => {
  return <SuccessfulOrder />;
};

// Checkout.getLayout = (page: ReactElement) => <>{page}</>;

export default SucessfullOrder;
