import React from 'react';

import { CartPage } from 'modules/CartPage';

const Cart = () => {
  return <CartPage />;
};

// Cart.getLayout = (page: ReactElement) => <>{page}</>;

export default Cart;
